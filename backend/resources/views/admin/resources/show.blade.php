@extends('admin.layouts.app')
@section('title',$resource['singular'].' #'.$item->getKey())
@section('page_title',$resource['singular'].' #'.$item->getKey())
@section('content')
<div class="d-flex justify-content-end gap-2 mb-3"><a class="btn btn-outline-primary" href="{{ route('admin.resources.edit',[$resourceKey,$item->getKey()]) }}"><i class="bi bi-pencil me-1"></i>Editar</a><form method="POST" action="{{ route('admin.resources.destroy',[$resourceKey,$item->getKey()]) }}" data-confirm-delete="Excluir este registro?">@csrf @method('DELETE')<button class="btn btn-outline-danger"><i class="bi bi-trash me-1"></i>Excluir</button></form></div><div class="card form-card"><div class="card-body p-4"><div class="row g-3">@foreach($item->getAttributes() as $key=>$raw)@php($value=$item->{$key})<div class="col-md-6"><div class="small text-secondary text-uppercase mb-1">{{ str_replace('_',' ',$key) }}</div>@if(is_array($value))<pre class="json">{{ json_encode($value,JSON_PRETTY_PRINT|JSON_UNESCAPED_UNICODE|JSON_UNESCAPED_SLASHES) }}</pre>@elseif($value instanceof \BackedEnum)<span class="badge badge-soft">{{ $value->value }}</span>@elseif($value instanceof \DateTimeInterface)<div>{{ $value->format('d/m/Y H:i:s') }}</div>@elseif(is_bool($value))<span class="badge {{ $value?'text-bg-success':'text-bg-secondary' }}">{{ $value?'Sim':'Não' }}</span>@else<div class="text-break">{{ $value }}</div>@endif</div>@endforeach</div></div></div>
@endsection
