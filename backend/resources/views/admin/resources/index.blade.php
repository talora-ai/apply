@extends('admin.layouts.app')
@section('title',$resource['label'])
@section('page_title',$resource['label'])
@section('content')
<div class="d-flex flex-wrap gap-2 justify-content-between mb-3"><form class="d-flex gap-2" method="GET"><input class="form-control" name="q" value="{{ request('q') }}" placeholder="Pesquisar..."><button class="btn btn-outline-secondary"><i class="bi bi-search"></i></button></form><a href="{{ route('admin.resources.create',$resourceKey) }}" class="btn btn-primary"><i class="bi bi-plus-lg me-1"></i>Novo {{ mb_strtolower($resource['singular']) }}</a></div>
<div class="card table-card"><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead class="table-light"><tr>@foreach($resource['columns'] as $column)<th>{{ str_replace('_',' ',mb_strtoupper($column)) }}</th>@endforeach<th class="text-end">Ações</th></tr></thead><tbody>@forelse($items as $item)<tr>@foreach($resource['columns'] as $column)<td>@php($value=$item->{$column})@if($value instanceof \BackedEnum)<span class="badge badge-soft">{{ $value->value }}</span>@elseif(is_bool($value))<span class="badge {{ $value?'text-bg-success':'text-bg-secondary' }}">{{ $value?'Sim':'Não' }}</span>@elseif($value instanceof \DateTimeInterface){{ $value->format('d/m/Y H:i') }}@elseif(is_array($value))<code>{{ \Illuminate\Support\Str::limit(json_encode($value),60) }}</code>@else{{ \Illuminate\Support\Str::limit((string)$value,80) }}@endif</td>@endforeach<td class="text-end text-nowrap"><a class="btn btn-sm btn-outline-secondary" href="{{ route('admin.resources.show',[$resourceKey,$item->getKey()]) }}"><i class="bi bi-eye"></i></a><a class="btn btn-sm btn-outline-primary" href="{{ route('admin.resources.edit',[$resourceKey,$item->getKey()]) }}"><i class="bi bi-pencil"></i></a><form class="d-inline" method="POST" action="{{ route('admin.resources.destroy',[$resourceKey,$item->getKey()]) }}" data-confirm-delete="Excluir {{ $resource['singular'] }} #{{ $item->getKey() }}?">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger"><i class="bi bi-trash"></i></button></form></td></tr>@empty<tr><td colspan="{{ count($resource['columns'])+1 }}" class="text-center text-secondary py-5">Nenhum registro encontrado.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $items->links() }}</div>
@endsection
